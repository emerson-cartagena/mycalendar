name: Deploy Supabase Functions

on:
  push:
    branches:
      - main
    paths:
      - 'supabase/functions/**'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install Supabase CLI
        run: |
          curl -fsSL https://github.com/supabase/cli/releases/download/`curl -s https://api.github.com/repos/supabase/cli/releases/latest | grep -oP '"tag_name": "\K(.*)(?=")'`/supabase_linux_amd64.tar.gz -o supabase.tar.gz || \
          curl -fsSL https://github.com/supabase/cli/releases/download/v1.198.0/supabase_linux_amd64.tar.gz -o supabase.tar.gz
          tar -xzf supabase.tar.gz
          sudo mv supabase /usr/local/bin/
          supabase --version

      - name: Deploy Edge Functions
        run: |
          supabase functions deploy --project-ref vrggahqfapozygajklaj
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
